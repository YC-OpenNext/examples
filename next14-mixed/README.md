# next14-mixed

Next.js 14 fixture for mixed router validation.

## Run

```bash
npm install
next-yc deploy --project . --verbose
```

Build is executed by `next-yc deploy` automatically.
Use `--dry-run` to run the same pipeline without cloud mutations.

Runtime config placeholder is provided in `next-yc-cfg.json`.
